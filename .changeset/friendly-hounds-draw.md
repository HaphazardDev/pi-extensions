---
"@haphazarddev/pi-interactive-code-review": patch
---

Simplify the review UI controls display by collapsing the footer to a `?` help hint and showing the full controls reference on demand in the lower section. Also make the diff view height-aware so it uses more of the available terminal space while staying within the viewport when comments or help are visible.
