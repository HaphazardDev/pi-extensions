---
"@haphazarddev/pi-interactive-code-review": patch
---

Add a searchable changed-file picker to `/review` with fuzzy filename matching, per-file change summaries, match highlighting, and direct keyboard navigation into the diff viewer. Also add in-file diff search with highlighted matches and next/previous match navigation.
